import { DefaultTheme } from "styled-components";
import { colors } from "./colors";
import { fonts } from "./fonts";
import { gradients } from "./gradietns";
import { consts } from "./consts";

export const Theme: DefaultTheme = {
  colors,
  fonts,
  gradients,
  consts
}